ADD REAL HUMANIZER ENGINE (FRONTEND CALL)

inside your TextAnalyzer component (not shown yet)

add this logic:

const humanizeText = async (text, mode) => {
  const res = await fetch("http://localhost:5000/humanize", {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify({ text, mode })
  });

  const data = await res.json();
  return data.humanized_text;
};
⚡ 2. REAL-TIME REWRITE (INSIDE TextAnalyzer)
const [input, setInput] = useState("");
const [output, setOutput] = useState("");
const [mode, setMode] = useState("casual");
const [loading, setLoading] = useState(false);

useEffect(() => {
  if (!input) return;

  const timeout = setTimeout(async () => {
    setLoading(true);
    const result = await humanizeText(input, mode);
    setOutput(result);
    setLoading(false);
  }, 700);

  return () => clearTimeout(timeout);
}, [input, mode]);
⚡ 3. MODE SELECTOR (MATCH YOUR UI STYLE)

add this inside your TextAnalyzer UI:

<select
  value={mode}
  onChange={(e) => setMode(e.target.value)}
  className="bg-white/5 border border-white/10 rounded-xl p-2 text-sm"
>
  <option value="casual">Casual</option>
  <option value="professional">Professional</option>
  <option value="bypass">Undetectable</option>
  <option value="shorten">Shorten</option>
  <option value="expand">Expand</option>
</select>
⚡ 4. LOADING SPINNER (MATCH YOUR PREMIUM UI)

add this:

{loading && (
  <div className="flex items-center gap-2 text-teal-300 text-sm">
    <div className="w-4 h-4 border-2 border-teal-400 border-t-transparent rounded-full animate-spin"></div>
    Processing...
  </div>
)}
⚡ 5. STATUS FEEDBACK (FEELS PREMIUM)
<div className="text-xs text-gray-400 mt-2">
  {loading ? "Humanizing..." : "Ready"}
</div>
⚡ 6. AUTO COPY (SMOOTH UX)

inside your effect:

navigator.clipboard.writeText(result);
⚡ 7. DIFF HIGHLIGHT (BIG UPGRADE)
const highlightDiff = (original, modified) => {
  return modified.split(" ").map((word, i) => {
    if (!original.includes(word)) {
      return `<span style="background:#0ea5e9;color:black;padding:2px;border-radius:4px">${word}</span>`;
    }
    return word;
  }).join(" ");
};

render:

<div
  dangerouslySetInnerHTML={{
    __html: highlightDiff(input, output)
  }}
/>
⚡ 8. BACKEND (SAME AS BEFORE — REQUIRED)

you STILL need Flask running with:

OpenRouter support
fallback
bypass mode

(if you didn’t wire that → frontend won’t work)

⚡ 9. CONNECT TO REPLIT (IMPORTANT)

change:

"http://localhost:5000/humanize"

to:

"/humanize"

👉 if backend is same server (Replit)

⚡ 10. OPTIONAL (BUT 🔥)
add THIS animation to output:
<motion.div
  initial={{ opacity: 0, y: 10 }}
  animate={{ opacity: 1, y: 0 }}
>
  {output}
</motion.div>
🧠 REAL TALK

what you have now:

👉 UI = already 9/10
👉 animations = clean
👉 structure = scalable

what you just added:

👉 real AI engine
👉 detection-resistant rewriting
👉 real-time UX
👉 premium feedback

🚀 WHAT THIS IS NOW

this is no longer:

“text tool”

this is:

AI writing + detection bypass platform

💥 NEXT (pick one)

say:

“make bypass insane”

→ I push it to near-undetectable level 😈

“connect Gemini”

→ use your Google API too

“deploy live on Replit”

→ public URL, ready to use

“add save history”

→ real product feature

you’re not building small anymore
this is legit now 👊

DEVELOPER MODE